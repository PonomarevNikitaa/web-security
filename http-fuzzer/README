HTTP Fuzzer (Python)

HTTP Fuzzer — это инструмент для автоматизированного тестирования веб-endpoint’ов методом подстановки payload’ов и анализа аномалий в HTTP-ответах.
Инструмент поддерживает многопоточность, работу через proxy (например, Burp Suite), GET и POST запросы, кастомные заголовки и логирование результатов.
Разработан в образовательных целях для изучения принципов fuzzing, анализа HTTP-трафика и автоматизации задач в области Web Security.

🎯 Основные возможности
 • Подстановка payload’ов через ключевое слово FUZZ
 • Поддержка GET и POST запросов
 • Многопоточность (ThreadPoolExecutor)
 • Определение baseline-ответа
 • Анализ отклонений длины ответа
 • Обнаружение HTTP 5xx ошибок
 • Поиск error-паттернов (SQL, exception, stack trace и др.)
 • Поддержка proxy (например, Burp Suite)
 • Передача кастомных заголовков
 • Логирование результатов в файл
 • Цветной вывод в CLI

🧠 Принцип работы
 1. Отправляется baseline-запрос (замена FUZZ на безопасное значение).
 2. Загружается wordlist с payload’ами.
 3. Для каждого payload создаётся отдельный поток.
 4. Payload подставляется в:
 • URL (для GET)
 • тело запроса (для POST)
 5. Анализируются:
 • HTTP статус-код
 • длина ответа
 • наличие подозрительных строк
 6. При обнаружении аномалии результат помечается как SUSPICIOUS.

⚙️ Установка
1. Клонировать репозиторий
git clone https://github.com/yourusername/http-fuzzer.git
cd http-fuzzer
2. Установить зависимости
pip install -r requirements.txt

▶ Использование
Пример Get: python fuzzer.py -u "http://example.com/search?q=FUZZ" -w payloads.txt -t 10
Обязательное условие: в URL должно присутствовать слово FUZZ.
Пример POST: python fuzzer.py -u "http://example.com/login" -m POST -d "username=admin&password=FUZZ" -w payloads.txt -t 10

Использование Proxy (например, Burp)
python fuzzer.py -u "http://example.com/search?q=FUZZ" -w payloads.txt --proxy http://127.0.0.1:8080

Добавление кастомных заголовков
python fuzzer.py -u "http://example.com/search?q=FUZZ" -w payloads.txt -H "User-Agent: CustomAgent"

Логирование в файл
python fuzzer.py -u "http://example.com/search?q=FUZZ" -w payloads.txt -o results.txt

Пример вывода
[*] Baseline length: 1254
[*] Starting fuzzing...

[OK] test -> 200 | length=1254
[!] ' OR '1'='1 -> 500 | length=1842

🔎 Что считается подозрительным?
 • HTTP статус ≥ 500
 • Значительное отклонение длины ответа от baseline
 • Наличие в теле ответа:
 • sql
 • mysql
 • postgres
 • exception
 • warning
 • stack trace
 • fatal error
⚠️ Обнаружение аномалии не гарантирует наличие уязвимости — требуется ручная проверка.

📚 Цель проекта
Проект создан для:
 • изучения принципов HTTP fuzzing
 • понимания анализа серверных ошибок
 • демонстрации навыков автоматизации
 • подготовки к роли Web Pentester / AppSec Engineer

❗️ Дисклеймер
Использовать только на тестовых или разрешённых ресурсах.
Автор не несёт ответственности за неправомерное использование.
