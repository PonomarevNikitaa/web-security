Subdomain Scanner (Python)
📌 Описание
Subdomain Scanner — это многопоточный инструмент для перебора и проверки доступности поддоменов целевого домена.
Инструмент предназначен для образовательных целей и изучения методов reconnaissance (разведки) в рамках тестирования безопасности веб-приложений.
Поддерживает работу через proxy (например, Burp Suite), логирование результатов и настройку количества потоков.

🎯 Основные возможности
 • Многопоточный перебор поддоменов (ThreadPoolExecutor)
 • Проверка доступности через HTTP-запрос
 • Поддержка proxy (Burp Suite)
 • Настраиваемое количество потоков
 • Логирование найденных поддоменов в файл
 • Игнорирование комментариев в wordlist
 • Цветной CLI-вывод

🧠 Принцип работы
 1. Загружается wordlist с возможными именами поддоменов.
 2. Для каждого элемента формируется полный домен:
subdomain + “.” + target_domain
 3. Каждый поддомен проверяется через HTTP-запрос.
 4. Если сервер отвечает — поддомен считается найденным.
 5. Результат выводится в терминал и (при необходимости) записывается в файл.

⚙️ Установка
1. Клонировать репозиторий
git clone https://github.com/yourusername/subdomain-scanner.git
cd subdomain-scanner

2. Установить зависимости
pip install -r requirements.txt

Зависимости:
 • requests
 • colorama

▶️ Использование
🔹 Базовый пример
python scanner.py -d example.com -w wordlist.txt

🔹 Указание количества потоков 
python scanner.py -d example.com -w wordlist.txt -t 20

🔹 Использование proxy (например, Burp Suite)
python scanner.py -d example.com -w wordlist.txt --proxy http://127.0.0.1:8080

🔹 Логирование результатов в файл
python scanner.py -d example.com -w wordlist.txt -o results.txt

🧪 Пример вывода
[*] Scanning example.com with 10 threads...

[FOUND] www.example.com -> 200
[FOUND] admin.example.com -> 403
[FOUND] api.example.com -> 301

[*] Scan completed.

📂 Формат wordlist
Файл wordlist может содержать:
 • имена поддоменов
 • пустые строки
 • комментарии (начинаются с #)

Пример:
# common subdomains
www
admin
dev
test
api

🔎 Ограничения текущей версии
 • Проверка осуществляется только через HTTP
 • Нет DNS-проверки (только HTTP-валидация)
 • Нет поддержки HTTPS-отдельной логики
 • Нет анализа заголовков ответа
 • Нет brute-force по wildcard-доменам

🚀 Возможные улучшения
 • Добавление DNS-резолвинга (socket / dnspython)
 • Поддержка HTTPS и fallback-логики
 • Анализ заголовков (Server, X-Powered-By)
 • Автоматическое определение wildcard-домена
 • Поддержка rate limit detection
 • Асинхронная версия (asyncio)

❗️ Дисклеймер
Использовать только на разрешённых или тестовых ресурсах.
Автор не несёт ответственности за неправомерное использование
